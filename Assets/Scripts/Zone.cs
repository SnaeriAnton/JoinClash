using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class Zone : MonoBehaviour
{
    //[SerializeField] private Transform _transform;

    ////private float _radius = 1.8f;
    //private float _radius = 2f;

    //public Vector3 GetVectorMagnitude(Vector3 position)
    //{
    //    Vector3 offset = position - _transform.position;
    //    offset = new Vector3(offset.x, 0, offset.y);
    //    Vector3 vector = _transform.position + Vector3.ClampMagnitude(offset, _radius);
    //    return vector;
    //}


}
